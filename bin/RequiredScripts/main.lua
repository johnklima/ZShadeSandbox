--Global value to increment the story in the game as it progresses
story = 0

--Main Entry file for the game
function main()
    --EXAMPLE
	--Load the sounds in the game
	--local intro_music = "title.mid"
	--load_music(intro_music)
	--play_music(intro_music)
	--load_sound("sound01.wav")
	--play_sound("sound01.wav")
	--sprite = Sprite:new()
	--sprite:setHealth(100)
	--win_print("LUA: Sprite's health is "..sprite:getHealth());
	--if (key_pressed("KEY_UP")) then
	--	win_print("Up arrow pressed in main.lua")
	--end
	--if (in_editor() == false) then
	--	--Name of starting world and map
	--	local world_name = "FoxIsland"
	--	local starting_map_name = "FoxHunterIsland"
	--	local xml_world_name = "FoxHunterWorld"
	--	
	--	--Load the world into the game which loads all the maps
	--	load_world(world_name, xml_world_name)
	--	
	--	--Set the starting map
	--	set_render_map(world_name, starting_map_name)
    --end
    --Load the Main Menu
    --load_main_menu("MainMenu1")
    --Make sure we are in the main menu state
    --set_state("MAINMENU")
end